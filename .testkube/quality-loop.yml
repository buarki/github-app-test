# Scenario 3: pr-integration-fixture requires pkg/** to run
# Expected: PR touching README.md only runs pr-unit-fixture (integration excluded by matcher)
workflows:
  - name: pr-unit-fixture
    on: [pull_request]
    required: true

  - name: pr-integration-fixture
    on: [pull_request]
    paths: ["pkg/**"]
    required: true

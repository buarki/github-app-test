# Scenario 5: pr-flaky-fixture is required=false (fails on purpose)
# Expected: parent has 2 steps.
#   - required-user-workflows: 2 workflows in parallel, both pass, step passes
#   - optional-user-workflows: 1 workflow, fails, but Optional=true so parent status not affected
# Quality gate = passed, PR not blocked.
workflows:
  - name: pr-unit-fixture
    on: [pull_request]
    required: true

  - name: pr-integration-fixture
    on: [pull_request]
    required: true

  - name: pr-flaky-fixture
    on: [pull_request]
    required: false

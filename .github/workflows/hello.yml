name: hello

on:
  push:
    branches: [main, teste-1]
  workflow_dispatch:
    inputs:
      reason:
        description: "why this dispatch"
        required: false

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: greeting
        run: |
          echo "hello world"
          echo "ref: $GITHUB_REF"
          echo "sha: $GITHUB_SHA"
          echo "trigger: $GITHUB_EVENT_NAME"
